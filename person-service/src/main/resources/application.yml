spring:
  application:
    name: person-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: person-service
  docker:
    compose:
      lifecycle-management: start_only

server:
  port: 8082
