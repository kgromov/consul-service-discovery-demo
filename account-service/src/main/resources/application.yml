spring:
  application:
    name: account-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        heartbeat:
          enabled: true
  docker:
    compose:
      lifecycle-management: none
server:
  port: 8081
